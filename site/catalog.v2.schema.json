{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://addons.appdevforall.org/v2/catalog.schema.json",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "schemaVersion",
    "generated",
    "addons"
  ],
  "properties": {
    "schemaVersion": {
      "const": 2
    },
    "generated": {
      "type": "string"
    },
    "addons": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/addon"
      }
    }
  },
  "$defs": {
    "addon": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "type",
        "slug",
        "addonId",
        "name",
        "version",
        "summary",
        "description",
        "origin",
        "license",
        "tags",
        "author",
        "minAppVersion",
        "iconUrl",
        "iconDarkUrl",
        "pageUrl",
        "sourceUrl",
        "download"
      ],
      "properties": {
        "type": {
          "enum": [
            "plugin",
            "template",
            "snippet",
            "code-action"
          ]
        },
        "slug": {
          "type": "string",
          "pattern": "^[a-z0-9]+(-[a-z0-9]+)*$"
        },
        "addonId": {
          "type": "string",
          "minLength": 1
        },
        "name": {
          "type": "string",
          "minLength": 1
        },
        "version": {
          "type": "string",
          "pattern": "^[0-9]+(\\.[0-9]+)*$"
        },
        "summary": {
          "type": "string",
          "maxLength": 120
        },
        "description": {
          "type": "string"
        },
        "origin": {
          "enum": [
            "appdevforall",
            "community"
          ]
        },
        "license": {
          "type": "string"
        },
        "tags": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "author": {
          "type": "object",
          "additionalProperties": false,
          "required": [
            "name",
            "url"
          ],
          "properties": {
            "name": {
              "type": "string"
            },
            "url": {
              "type": "string"
            }
          }
        },
        "minAppVersion": {
          "type": "string"
        },
        "iconUrl": {
          "type": "string"
        },
        "pageUrl": {
          "type": "string"
        },
        "sourceUrl": {
          "type": "string"
        },
        "download": {
          "$ref": "#/$defs/file"
        },
        "sourceTarball": {
          "$ref": "#/$defs/file"
        },
        "iconDarkUrl": {
          "type": "string"
        }
      }
    },
    "file": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "url",
        "sha256",
        "size"
      ],
      "properties": {
        "url": {
          "type": "string"
        },
        "sha256": {
          "type": "string",
          "pattern": "^[a-f0-9]{64}$"
        },
        "size": {
          "type": "integer",
          "minimum": 0
        }
      }
    }
  },
  "$comment": "Version 2 (ADFA-6252) makes two changes section 10.5 forbids within a major version, which is why it is a new major published alongside v1 rather than over it. pluginId became addonId: the catalog carries four addon types and the name fitted one. sourceTarball left the required list: a template ships none, because its .cgt is plain text throughout and the download already is the source, while sourceUrl still names the directory. A v2 consumer must treat sourceTarball as optional. Everything else is as v1, and v1 keeps being published unchanged for fielded app builds that cannot be updated."
}
